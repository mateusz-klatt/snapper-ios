# Files exempt from the make ios-i18n-check rule (v1 single-pattern).
# Each non-comment, non-blank line is a substring match against the
# offending file path; any match causes the hit to be ignored.
#
# When the v2 multi-pattern lint (check_i18n_strict.sh) activates at
# Phase J completion, this file may be reformatted to the v2 spec
# `<path>:<line>:<reason>` form documented in the strict-script
# header. Until then, lines are substring prefixes per v1 semantics.

Snapper/I18n/LocaleStrings.swift
SnapperTests/
